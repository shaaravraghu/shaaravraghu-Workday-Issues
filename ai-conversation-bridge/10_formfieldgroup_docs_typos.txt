Suggested Title: Fix typos and outdated wording in FormFieldGroup inline documentation
Suggested Template: chore
Suggested Labels: chore

## 🧹 Chore

Clean up the inline docs in `modules/react/form-field/lib/FormFieldGroup.tsx`.

Observed issues include:
- `FormFiledGroup` typo in example code
- `FromFieldGroup` typo in multiple example lines
- a stale TODO that still says `horizontal` will be removed in v13

Because these comments are part of developer-facing guidance and may feed generated docs, fixing them would reduce confusion for consumers and keep the API documentation aligned with the current major version.
